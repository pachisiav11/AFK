# AFK backend dependencies.
#
# Phase 1 — core (stdlib only).
#
# Phase 2 — audio capture + Parakeet 0.6B v3 STT (CPU):
numpy>=1.26,<2.3
sounddevice>=0.4.6
soundfile>=0.12.1
#
# ASR engine A (default, light): Parakeet 0.6B v3 via ONNX Runtime.
onnx-asr[cpu,hub]>=0.6.0
#
# ASR engine B (optional, official): NVIDIA NeMo + PyTorch (CPU).
# Heavier (~3GB of deps) but runs the official .nemo checkpoint. Installed
# on demand; uncomment if you want it provisioned by setup-python.js:
#   nemo_toolkit[asr]>=2.7
#
# Phase 3 — global hotkeys (press + release for push-to-talk) + clipboard/paste:
pynput>=1.7.6
pyperclip>=1.8.2
#
# Phase 4 — Clarify (Gemma GGUF on CPU).
# Inference runs through the official llama.cpp `llama-server` binary (vendored
# under vendor/llama.cpp), NOT llama-cpp-python: the prebuilt python wheels
# require AVX-512, which consumer Raptor Lake CPUs (e.g. i7-1360P) lack. The
# upstream binary ships per-microarch CPU backends and picks AVX2 at runtime.
# No extra Python package required (we call the server over HTTP via stdlib).
# Short model: Gemma 3 270M IT; long model: Gemma 4 E2B IT.
